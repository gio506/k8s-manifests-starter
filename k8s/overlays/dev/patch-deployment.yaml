apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: demo-web
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: nginx
          env:
            - name: APP_STAGE
              value: dev
